<div class="haikei2">
  <div class="container py-5">
    <div class="row">
      <div class="col">
        <div class="form-memory py-3 bg-white">
          <div class="row my-4 offset-2">
            <h2 class="midasi">New Memory</h2>
          </div>
          <%= render 'layouts/errors', obj: @blog %>
          <%= form_with model: @blog, local:true do |f| %>
            <h4>Imaage</h4>
            <%= f.attachment_field :image %>
            <h4>Title</h4>
            <%= f.text_field :title, placeholder: "タイトル" %>
            <h4>Description</h4>
            <%= f.text_area :description, placeholder: "記録内容" %>
            <h4>Address</h4>
            <%= f.text_field :address, placeholder: "住所" %>
            <h4>Country</h4>
            <%= f.country_select :country_name, priority_countries: ["JP", "US"] %><br>
            <div class="my-4">
            <%= f.submit 'Create Memory', class: "button-s" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>